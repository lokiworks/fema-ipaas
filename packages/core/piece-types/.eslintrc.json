{
  "extends": ["../../../.eslintrc.json"],
  "ignorePatterns": ["!**/*", "dist"],
  "overrides": [
    {
      "files": ["*.ts", "*.tsx"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "patterns": [
              {
                "group": [
                  "@fema/shared",
                  "@fema/server*",
                  "@fema/pieces*",
                  "@fema/web*"
                ],
                "message": "core/* packages are the framework-agnostic foundation; they must not import shared, server, pieces, or web. core-piece-types may depend only on core-utils."
              }
            ]
          }
        ]
      }
    }
  ]
}
