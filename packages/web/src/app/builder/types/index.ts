export enum RightSideBarType {
  NONE = 'none',
  CONNECTOR_SETTINGS = 'connector-settings',
  VERSIONS = 'versions',
  RUNS = 'runs',
}

export enum ChatDrawerSource {
  TEST_FLOW = 'test-flow',
  TEST_STEP = 'test-step',
}
