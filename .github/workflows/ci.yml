name: CI
on:
  pull_request:

permissions:
  actions: read
  contents: read


jobs:
  lint:
    runs-on: ubuntu-latest
    if: github.repository == 'lokiworks/fema-ipaas'
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-

      - run: bun install

      - name: Turbo Cache
        uses: rharkor/caching-for-turbo@v2.4.2
        with:
          provider: s3
          s3-bucket: ${{ secrets.TURBO_CACHE_S3_BUCKET }}
          s3-region: auto
          s3-endpoint: ${{ secrets.TURBO_CACHE_S3_ENDPOINT }}
          s3-access-key-id: ${{ secrets.TURBO_CACHE_S3_ACCESS_KEY_ID }}
          s3-secret-access-key: ${{ secrets.TURBO_CACHE_S3_SECRET_ACCESS_KEY }}
          max-age: 3w
          max-size: 20gb

      - name: Check if framework or common connectors are changed
        id: check-framework-common
        run: |
          CHANGED_FILES="$(git diff --name-only HEAD origin/main | tr '\n' ' ')"
          if echo "$CHANGED_FILES" | grep -q "connectors/sdk\|connectors/common"; then
            echo "framework_or_common_changed=true" >> $GITHUB_OUTPUT
          else
            echo "framework_or_common_changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Lint core projects
        run: npm run lint-core

      - name: Lint all connectors projects
        if: steps.check-framework-common.outputs.framework_or_common_changed == 'true'
        run: npm run lint-connectors

  main:
    runs-on: ubuntu-latest
    if: github.repository == 'lokiworks/fema-ipaas'
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-

      - run: bun install

      - name: Turbo Cache
        uses: rharkor/caching-for-turbo@v2.4.2
        with:
          provider: s3
          s3-bucket: ${{ secrets.TURBO_CACHE_S3_BUCKET }}
          s3-region: auto
          s3-endpoint: ${{ secrets.TURBO_CACHE_S3_ENDPOINT }}
          s3-access-key-id: ${{ secrets.TURBO_CACHE_S3_ACCESS_KEY_ID }}
          s3-secret-access-key: ${{ secrets.TURBO_CACHE_S3_SECRET_ACCESS_KEY }}
          max-age: 3w
          max-size: 20gb

      - name: Get changed files
        id: changed-files
        run: echo "files=$(git diff --name-only HEAD origin/main | tr '\n' ' ')" >> $GITHUB_OUTPUT

      - name: Check if framework or common connectors are changed
        id: check-framework-common
        run: |
          CHANGED_FILES="${{ steps.changed-files.outputs.files }}"
          if echo "$CHANGED_FILES" | grep -q "connectors/sdk\|connectors/common"; then
            echo "framework_or_common_changed=true" >> $GITHUB_OUTPUT
          else
            echo "framework_or_common_changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Extract connectors package names from changed files
        id: extract-connectors
        run: |
          CONNECTORS=$(echo "${{ steps.changed-files.outputs.files }}" | grep -oE "packages/connectors/(community|core)/[^/]+" | awk -F'/' '{print $NF}' | sort -u | tr '\n' ',' | sed 's/,$//')
          if [ -n "$CONNECTORS" ]; then
            FILTERS=$(echo "$CONNECTORS" | tr ',' '\n' | sed 's/.*/{&}/' | tr '\n' ',' | sed 's/,$//')
            echo "connectors_filters=$FILTERS" >> $GITHUB_OUTPUT
          else
            echo "connectors_filters=" >> $GITHUB_OUTPUT
          fi

      - name: Build core projects
        run: npx turbo run build --filter=web --filter=worker --filter=api --filter=@fema-ipaas/engine --filter=@fema-ipaas/connector-http --filter=@fema-ipaas/connector-data-mapper --filter=@fema-ipaas/connector-approval --filter=@fema-ipaas/connector-webhook --filter=@fema-ipaas/connector-delay

      - name: Build changed connectors projects
        if: steps.extract-connectors.outputs.connectors_filters != '' && steps.check-framework-common.outputs.framework_or_common_changed == 'false'
        run: |
          IFS=',' read -ra CONNECTORS <<< "${{ steps.extract-connectors.outputs.connectors_filters }}"
          FILTER_ARGS=""
          for connector in "${CONNECTORS[@]}"; do
            connector_name=$(echo "$connector" | tr -d '{}')
            if [ -f "packages/connectors/community/${connector_name}/package.json" ] || [ -f "packages/connectors/core/${connector_name}/package.json" ]; then
              FILTER_ARGS="$FILTER_ARGS --filter=@fema-ipaas/connector-${connector_name}"
            fi
          done
          if [ -n "$FILTER_ARGS" ]; then
            npx turbo run build $FILTER_ARGS
          fi

      - name: Build all connectors projects
        if: steps.check-framework-common.outputs.framework_or_common_changed == 'true'
        run: npx turbo run build --filter='@fema-ipaas/connector-*'

      - name: Run all tests and migration checks in parallel
        run: |
          set -euo pipefail
          pids=()

          npx turbo run test --filter=@fema-ipaas/engine --filter=@fema-ipaas/shared --filter=@fema-ipaas/sandbox --filter=@fema-ipaas/ai-providers --filter=@fema-ipaas/connector-sdk --filter=web &
          pids+=($!)

          npx turbo run test-ce test-ee test-cloud check-migrations --filter=api &
          pids+=($!)

          npx tsx tools/scripts/check-migration-rollback.ts &
          pids+=($!)

          status=0
          for pid in "${pids[@]}"; do
            if ! wait "$pid"; then
              status=1
            fi
          done
          exit $status

  # Runs ONLY the tool-search integration tests against a real PostgreSQL + pgvector
  # service container. The default `main` job above runs these on PGLITE, which papers
  # over node-postgres-specific driver behaviour (e.g. TypeORM `.query()` returning
  # `[rows, count]` instead of a plain rows array). This job exists to catch that class
  # of bug. It is intentionally scoped and additive — it does NOT change how the main
  # suite runs.
  tool-search-postgres:
    name: tool-search (postgres)
    runs-on: ubuntu-latest
    if: github.repository == 'lokiworks/fema-ipaas'
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: fema
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    env:
      # dotenv (vitest.setup.ts) loads .env.tests WITHOUT overriding values already
      # present in the environment, so these job-level vars win over the PGLITE defaults.
      FEMA_DB_TYPE: POSTGRES
      FEMA_POSTGRES_HOST: localhost
      FEMA_POSTGRES_PORT: "5432"
      FEMA_POSTGRES_DATABASE: fema
      FEMA_POSTGRES_USERNAME: postgres
      FEMA_POSTGRES_PASSWORD: postgres
      FEMA_POSTGRES_USE_SSL: "false"
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-

      - run: bun install

      - name: Turbo Cache
        uses: rharkor/caching-for-turbo@v2.4.2
        with:
          provider: s3
          s3-bucket: ${{ secrets.TURBO_CACHE_S3_BUCKET }}
          s3-region: auto
          s3-endpoint: ${{ secrets.TURBO_CACHE_S3_ENDPOINT }}
          s3-access-key-id: ${{ secrets.TURBO_CACHE_S3_ACCESS_KEY_ID }}
          s3-secret-access-key: ${{ secrets.TURBO_CACHE_S3_SECRET_ACCESS_KEY }}
          max-age: 3w
          max-size: 20gb

      - name: Enable pgvector extension
        # The pgvector image ships the extension binary but does not auto-create it in the
        # database. The tool_search_index migration skips table creation when `vector` is
        # absent, and the DB-only test never boots the server path that would CREATE it,
        # so create it explicitly before migrations run.
        run: |
          PGPASSWORD=postgres psql -h localhost -p 5432 -U postgres -d fema \
            -c 'CREATE EXTENSION IF NOT EXISTS vector;'

      - name: Build api
        run: npx turbo run build --filter=api

      - name: Run tool-search integration tests on Postgres
        working-directory: packages/server/api
        # --no-file-parallelism: these files all point at the one shared `fema`
        # service DB and each runs migrations on `DataSource.initialize()`. Vitest's default
        # forked parallelism runs those migration passes concurrently, which collide on
        # shared catalog objects (the migrations table, en_natural collation, added columns)
        # and fail non-deterministically. Serial execution migrates once; later files skip
        # via the migrations table. (PGLITE gives each file its own DB, so the default suite
        # is unaffected — only this shared-Postgres job needs serializing.)
        run: npx vitest run test/integration/ce/tool-search --bail 1 --passWithNoTests=false --no-file-parallelism
