apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fema.fullname" . }}-test-connection"
  labels:
    {{- include "fema.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fema.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
